project Default is
   for Main use ("main.adb", "test_arbre.adb", "test_sgf.adb");
   for Source_Dirs use ("src", "src/test", "src/arbre", "src/sgf");
   for Object_Dir use "obj";
   for Exec_Dir use "bin";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnata");
   end Compiler;
end Default;